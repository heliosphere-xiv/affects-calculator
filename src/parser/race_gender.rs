#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum Race {
    AuRa,
    Elezen,
    Highlander,
    Hrothgar,
    Hyur,
    Lalafell,
    Midlander,
    Miqote,
    Roegadyn,
    Viera,
}

#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum Gender {
    Female,
    Male,
}
